const initalState = {
    checking: true,
    // uid: null,
    // name: null
};

export const authReducer = ( state = initalState, action ) => {

    switch ( action.type ) {
    
        default:
            return state;
    }

};